import styled from 'styled-components'

export const Container = styled.div`
  button {
    color: var(--secondary);
    padding: 7px;
  }
`

export const Icon = styled.div`
  font-size: 22px;
  &.icon-house {
    color: var(--primary);
  }
`
