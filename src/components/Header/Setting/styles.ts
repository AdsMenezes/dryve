import styled from 'styled-components'

export const Container = styled.div`
  button {
    padding: 7px;
  }
`

export const Icon = styled.span`
  color: var(--secondary);
  font-size: 22px;
  position: relative;
`
